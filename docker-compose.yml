version: '3.8'

services:
  sql-server:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: weather-dw
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=Strong@Passw0rd2026!
    ports:
      - "1433:1433"
    volumes:
      - sql-data:/var/opt/mssql

volumes:
  sql-data: